#include "main.h"

void main() {

    startup();

    cls(CL_CYAN);

    // gui_draw_window("Nifed v zdanii", 32, 32, 320, 240);
    // fdc_read(0);

    int xp = -1, yp = -1;
    for(;;) {
        // ..
    }
}
